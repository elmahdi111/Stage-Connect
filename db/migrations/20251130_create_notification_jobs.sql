
-- Migration removed: notification jobs table not used because notifications are disabled.
-- If you need to re-enable notifications in the future, restore the original migration from source control.

